#include <stdio.h>

int main() {
	printf(%d 더하기 %d는 %d,1,2,3);
	//print + format
	//format string(서식문자열)을 출력하는 함수
	//서식문자열:format character(서식문자)가 포함된 문자열
	//서식문자:% + 알파벳문자
	//        :빈 칸과 같은 문자
}